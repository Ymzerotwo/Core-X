PORT=5000 #server port
# =============================================================================
# 🪵 Logger Configuration
# =============================================================================

# 1. Environment Mode
# - development: Logs are printed to the console with colors for readability.
# - production: Logs are written to files only (performance optimization).
NODE_ENV=development

# 2. Service Name
# This name appears in every log entry. Useful for distinguishing between
# multiple services if you aggregate logs centrally.
SERVICE_NAME=Core-X-Backend

# 3. Log Level
# Defines the minimum severity level to record.
# Priority order: error (0) > warn (1) > info (2) > http (3) > verbose (4) > debug (5)
# - debug: Logs everything (use for local development).
# - http: Logs API requests and errors (recommended for staging).
# - info: Logs important events and errors only (recommended for production).
LOG_LEVEL=debug

# 4. Force Console Logging (Optional)
# Set to 'true' to enable console output even in 'production' mode.
# Useful for debugging containers (e.g., Docker logs).
ENABLE_CONSOLE_LOGS=false

# =============================================================================
# ⚡ Supabase Configuration
# =============================================================================

# 1. Supabase API URL
# Found in Supabase Dashboard -> Settings -> API
SUPABASE_URL=https://your-project-id.supabase.co

# 2. Service Role Key (Admin Secret)
# ⚠️ WARNING: Never share this key on the client-side (Frontend).
# This key bypasses Row Level Security (RLS) and has full admin access.
# Found in Supabase Dashboard -> Settings -> API -> service_role secret
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 3. Public Anon Key (Optional for Backend)
# You might need this if you want to simulate a standard user action,
# but usually, the Service Role Key is enough for the backend.
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...


# =============================================================================
# 🔐 Security Configuration
# =============================================================================

# Secret key for signing cookies. Use a long, random string.
COOKIE_SECRET=your_super_secret_cookie_signing_key_here

# JWT Secret for local verification (Found in Supabase -> Settings -> API -> JWT Settings)
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# =============================================================================
# 🚀 Cluster Configuration
# =============================================================================
WORKERS_COUNT=1 # # Set to 1 to ensure accurate Rate Limiting without Redis 'full' for all cores, or specific number (e.g. 2)
ALLOWED_IPS=127.0.0.1,::1,::ffff:127.0.0.1

# =============================================================================
# 📦 Redis Configuration
# =============================================================================
REDIS_URL=redis://localhost:6379